# Core HTTP client (black-box calls to /chat endpoint)
httpx>=0.28.1

# DeepEval for hallucination + answer relevancy metrics
# Requires OPENAI_API_KEY at runtime. Set SKIP_LLM_METRICS=true to bypass.
deepeval>=0.21.0

# Testing
pytest>=8.3.4
pytest-asyncio>=0.25.0

# Utilities
python-dotenv>=1.0.1
